import { View, Text } from 'react-native';

export default function StudentAttendance() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-foreground text-xl font-bold">Attendance</Text>
      <Text className="text-muted-foreground mt-2">Coming soon to mobile...</Text>
    </View>
  );
}
